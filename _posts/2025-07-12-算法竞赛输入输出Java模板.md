---
title: 算法竞赛输入输出Java模板
date: 2025-07-12
categories: [ 数据结构与算法, 算法竞赛 ]
tags: [ 算法竞赛 ]
mermaid: true
order: 207
---

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws lOException {
        // 高效输入
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        // 高效输出
        PrintWriter out = new PrintWriter(new OutputStreamWriter(System.out));

        // 示例：读取一个整数 n
        int n = Integer.parseInt(br.readLine());

        /*
        处理多轮输入
        */
        while(line=br.readLine()!=null){//直到EOF
            if(line.trim().isEmpty())continue;//忽略空行
            /*
            示例：读取一行 n 个整数
            1 2 3
            */ 
            st = new StringTokenizer(br.readLine());
            int[] nums = new int[n];
            for (int i = 0; i < n; i++) {
                nums[i] = Integer.parseInt(st.nextToken());
            }
        }

        // 示例输出
        for (int i = 0; i < n; i++) {
            out.print(nums[i] + " ");
        }
        out.println(); // 换行
        out.flush();   // 刷新缓冲区

        /*
        如果不需要经常输出，直接使用System.out.print()即可
        */
    }
}
```